# available options for os are (ubuntu, arch, windows)
export OS='arch'

# available options for env are (home, digikala)
# export ENV='home'

# available options for system are (hulu, birikht, pal, labourer, fr, ir)
