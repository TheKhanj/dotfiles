#!/usr/bin/env sh

help() {
	cat <<-EOF
		Usage:
		  texttopdf [OUT-FILE]
	EOF
}

output="$1"

[ -z "$output" ] && help && exit 1

iconv -c -f utf-8 -t ascii//TRANSLIT - |
	enscript -B -p - -f "@12" |
	ps2""pdf - "$output"
